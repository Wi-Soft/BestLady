# TODO: Add more products and services

- [x] Update `myapp/migrations/0009_seed_items.py` so seed is idempotent (adds missing items even if DB already has items).

- [x] Add additional Cosmetics items (category=`cosmetic`).
- [x] Add additional Hair services (category=`hair`).
- [x] Add additional Nails services (category=`nails`).

- [x] Run Django migrations to apply seed changes.

- [ ] Verify pages:
  - `/` (home)
  - `/products/` and product detail
  - `/services/` and service detail

