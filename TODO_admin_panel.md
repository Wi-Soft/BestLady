# TODO: Admin panel improvements

- [ ] Update `myapp/admin.py` to enhance `ItemAdmin` list columns (add image/details_title).
- [ ] Add richer `list_filter` and `search_fields` for hair/nails optional fields.
- [ ] Add `fieldsets` for cleaner edit forms (common fields vs extra fields).
- [ ] Validate in Django Admin UI that list/search/edit work as expected.

